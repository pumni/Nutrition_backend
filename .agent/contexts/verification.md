Context release: agent-context-1.0.0
Derived from: `scripts/verify.ps1`, `scripts/verify-postgres.ps1`, `scripts/verify-agent-context.ps1`, `scripts/run-agent-verification.ps1`, `.agent/contracts/task-packet.schema.json`, `.agent/contracts/task-spec.schema.json`, `.agent/contracts/verification-report.schema.json`, `.agent/contracts/implementation-report.schema.json`, `.agent/contracts/external-evidence.schema.json`, `.agent/contracts/ci-attestation.schema.json`, `.agent/maps/verification-map.json`, `.agent/maps/ci-policy.json`, `.agent/evals/ci-cases.json`, `.agent/evals/implementation-report-cases.json`, `.agent/evals/task-spec-v2-cases.json`, `.agent/profiles/context-profiles.json`, `.agent/templates/task-spec.example.json`, `.github/workflows/agent-context-integrity.yml`, `.github/workflows/agent-task-attest.yml`, `schemas/parsed-meal-0.1.0.json`, `fixtures/vietnamese-meal-bench/manifest.json`, `fixtures/vietnamese-meal-bench/foundation-cases.json`, `deploy/compose.yaml`, `docs/archive/nutrition_backend_blueprint_v1.0/09_TESTING_EVALUATION_AND_OBSERVABILITY.md`
Use when: maintaining ACL verification, selecting repository gates, or validating context-layer changes.
Do not infer: that an ignored integration test is a pass for a missing PostgreSQL environment, or that a future benchmark gate is universally runnable.

The foundation verifier runs `cargo fmt --all -- --check`, workspace Clippy with warnings denied, workspace tests, JSON parsing, prohibited sensitive-log scanning, and Docker Compose validation. `scripts/verify-postgres.ps1` starts PostgreSQL, applies migrations and the test-only seed, runs ignored integration tests, exercises a bounded worker batch, smoke-tests the API contracts, and runs database immutability verification.

P10C's CI policy verifier validates the two fixed workflows without a YAML dependency: action commit pins, trigger isolation, default-branch dispatch, separate full-history checkouts, token permissions, trusted ControlRoot runner binding, temporary evidence paths, artifact retention, exact Rust toolchain, and absence of P10D capability export. The attestation workflow's `attest` job has contents read only; its separate `publish-status` job has statuses write only and no checkout.

ACL verification must validate manifest and context/release identities, profile uniqueness and references, source-register paths, source locks, byte budgets, empty decision points, non-empty gates, profile forbidden paths, the typed canonical gate registry, and task-scope changed paths. Task packets declare exact create/modify/delete sets; `allowed_paths` is only an outer boundary, deletion requires `delete_files`, and packet verification entries contain canonical gate IDs and `required` only. The trusted runner resolves execution from ControlRoot, validates an external task packet, writes task-state and verification evidence outside TargetRoot, and records bounded output metadata rather than raw command output. External benchmark evaluation is a release gate when named by a packet, not a universal local command.
