# Acceptance Report <WO-ID>

Reviewer status: `ACCEPTED` / `CHANGES_REQUESTED` / `BLOCKED_PROTECTED_DECISION` / `CANCELLED`

## Reviewed implementation

- Work order: `<WO-ID>`
- PR / branch: `<reference>`
- Reviewed commit: `<sha>`
- Handoff report: `<path>`

## Acceptance review

### Scope

- Write boundary respected: `YES` / `NO`
- Unrelated changes: `NONE` / `<details>`

### Acceptance criteria

- [ ] <criterion> — PASS / FAIL — <review evidence>
- [ ] <criterion> — PASS / FAIL — <review evidence>

### Verification

- Required canonical gates identified from actual diff: `YES` / `NO`
- Local evidence reviewed: `YES` / `NO`
- CI evidence reviewed when available: `YES` / `NO` / `N/A`
- Blocking failures: `NONE` / `<details>`

### Protected decisions

- Unapproved protected decision introduced: `NO` / `<details>`
- Security/privacy/provenance/fail-closed concerns: `NONE` / `<details>`

## Reviewer findings

### Blocking

- `<finding or NONE>`

### Non-blocking

- `<finding or NONE>`

## Decision

<Why the implementation is accepted, requires changes, or is blocked.>

`ACCEPTED` means the implementation satisfies this work order at the reviewed commit. It does **not** itself merge the PR or authorize production release.

## Required next action

- `<merge by human owner / return to Codex with requested changes / owner decision / none>`
